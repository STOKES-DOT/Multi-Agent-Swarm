Using the successful MoleculeEditor inspection and its stable AtomId/BondId values, echo the supplied authorization_id exactly and propose one complete `molecule_editor` `edit` transaction within the decoded command and fragment budgets. Interpret `parent_similarity_target` as the requested parent-child structural retention: values near 1 favor a local edit that preserves the current molecular framework, while values near 0 allow a larger scaffold-changing edit. This is only a target; the controller computes the realized RDKit Morgan/Tanimoto similarity after editing. If `tool_feedback` is present, the previous edit proposal failed: retain the same hypothesis, read the exact MoleculeEditor error code, choose a different site or operation that directly avoids that error, and do not repeat the rejected transaction. In particular, do not change one bond inside a fused aromatic system when the feedback reports `AROMATICITY_ERROR`, and use only neutral closed-shell fragments when it reports `CLOSED_SHELL_REQUIRED`. Do not guess identifiers, submit target/budget/policy fields, create disconnected intermediates, execute a spectrum or other scientific tool, or claim reward, fitness, or Evaluation authority. Return only schema-valid JSON.
