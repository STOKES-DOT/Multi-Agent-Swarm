Using the successful MoleculeEditor inspection and stable AtomId/BondId values, echo the supplied authorization_id exactly and propose one complete `molecule_editor` `edit` transaction. MoleculeEditor's exact nine operations are all permitted: `add_atom`, `remove_atom`, `replace_atom`, `add_bond`, `remove_bond`, `change_bond`, `attach_fragment`, `detach_fragment`, and `substitute_fragment`. The transaction must contain exactly `edit_command_target` commands and must include `required_primary_operation` when it is not null. Pair `add_atom` with a later `add_bond` through its transaction-local `@client_ref`; use remove/add bond combinations only when the final committed molecule remains connected. If any fragment operation is used, its fragment graphs must contain exactly `fragment_heavy_atom_target` heavy atoms in total. The controller will reject a valid-looking edit when realized parent similarity differs from `parent_similarity_target` by more than `parent_similarity_tolerance`, when fewer than `minimum_parent_heavy_atoms_changed` parent heavy atoms are structurally changed, or when net heavy-atom growth exceeds `max_net_heavy_atom_growth`. Therefore, low-similarity modes require genuine scaffold replacement rather than peripheral growth. Construct internally consistent ChemicalGraph hashes and serial counters. Do not submit target, budget, operation-policy, similarity-policy, or structural-policy fields; the controller adds those authoritative fields. The controller permits up to three distinct proposals. If `tool_feedback` is present, retain the same hypothesis but choose a different site, operation, or chemically distinct fragment that directly avoids the reported error without repeating a rejected transaction. After the third rejected proposal, the unchanged parent is evaluated as a fallback only and is excluded from pbest and gbest. Do not guess parent identifiers, create disconnected intermediates, run a scientific evaluator, or claim reward, fitness, or Evaluation authority. Return only schema-valid JSON.
The controller's seeded `required_operations` list takes precedence over raw weight ordering. Include every listed operation with its required multiplicity. Helpers such as `add_bond` and a scaffold-changing substitution are part of the same atomic transaction. Obtain fragment graphs via MoleculeEditor `inspect`; never invent hashes. Use `enumerate` for topology candidates only for its supported filters (replace_atom, add_bond, remove_bond, detach_fragment). Enumeration is a prefilter, not chemical validation. After a structural-policy rejection, revise the complete transaction; a policy miss does not mean the molecule is chemically invalid.
