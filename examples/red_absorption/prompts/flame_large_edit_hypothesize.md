Formulate one falsifiable molecular-edit hypothesis using the supplied read-only Wiki evidence and decoded target. MoleculeEditor's exact nine operations are all permitted: `add_atom`, `remove_atom`, `replace_atom`, `add_bond`, `remove_bond`, `change_bond`, `attach_fragment`, `detach_fragment`, and `substitute_fragment`. Treat `required_primary_operation` as an enforced operation that must appear when it is not null, and design exactly `edit_command_target` mutually compatible commands. An `add_atom` command must be paired with a later `add_bond` in the same transaction so no disconnected child is exposed. For fragment operations, use fragment graphs totaling exactly `fragment_heavy_atom_target` heavy atoms. `parent_similarity_target` is an enforced parent-child similarity objective with the supplied `parent_similarity_tolerance`, not a suggestion. When `minimum_parent_heavy_atoms_changed` is at least 10, propose a genuine scaffold-changing detach/substitute or mixed transaction that changes at least that many parent heavy atoms and respects `max_net_heavy_atom_growth`; do not satisfy the request by attaching a large peripheral substituent. Use atom and bond operations for chemically motivated heteroatom, connectivity, ring, or conjugation changes. If `previous_reflection` is present, use its revised mechanism without repeating a failed edit. FLAME/FLSF predicts solution-phase absorption, emission, PLQY, and molar extinction coefficient in dichloromethane. Absorption in 620-750 nm is the primary objective; PLQY and logarithmic epsilon are weak secondary terms. Echo the supplied authorization_id exactly, cite retrieved source paths and line ranges, label indirect design reasoning as an open hypothesis, and return only schema-valid JSON. Never claim reward, fitness, oscillator strength, TDDFT roots, or Evaluation authority.
Before editing, supply a short `mechanism` label and `minimum_change_nm` for the predicted absorption change. For `no_change`, the number is the largest allowed absolute change. These are predeclared model predictions, not measurements. Read `social_knowledge` as prior experimental context: distinguish model-supported predictions, refuted predictions, missing tests, and unproven explanations. Reinterpret transferable chemistry on `current_parent_smiles`; obtain all entity IDs from your own inspection. Only cite Wiki references retrieved in this request. The controller samples `required_operations` with the recorded seed; include its complete multiset, including dependency operations, within the exact command target.
