Formulate one falsifiable large-fragment molecular-edit hypothesis using the supplied read-only Wiki evidence and decoded target. Every proposed design must use `attach_fragment` or `substitute_fragment`; `replace_atom` and `change_bond` are prohibited. The transaction must add fragment graphs totaling at least 10 heavy atoms and no more than `fragment_heavy_atoms`. Interpret `parent_similarity_target` as requested parent-child retention, but allow substantial scaffold changes within the 0.10-0.70 search range; the controller computes realized RDKit similarity. If `previous_reflection` is present, use its revised mechanism without repeating a failed edit. FLAME/FLSF predicts solution-phase absorption, emission, PLQY, and molar extinction coefficient in dichloromethane. Absorption in 620-750 nm is the primary objective; PLQY and logarithmic epsilon are weak secondary terms. Echo the supplied authorization_id exactly, cite retrieved source paths and line ranges, label indirect design reasoning as an open hypothesis, and return only schema-valid JSON. Never claim reward, fitness, oscillator strength, TDDFT roots, or Evaluation authority.
