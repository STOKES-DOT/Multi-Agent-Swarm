Compare the evaluated large-fragment candidate with its immediate parent and determine separately whether the predicted direction was supported and whether the 620-750 nm target was reached. Every next-step recommendation and revised hypothesis must remain executable as an `attach_fragment` or `substitute_fragment` transaction with fragment graphs totaling at least 10 heavy atoms; single-atom replacements and bond-only changes are prohibited. Use the recorded rollback flag, parent similarity, FLAME/FLSF absorption, emission, PLQY, logarithmic epsilon, and reward without inventing values. Absorption-band proximity is the primary criterion; PLQY and logarithmic epsilon are weak secondary context. Treat a negative Stokes shift as a diagnostic warning, and state that FLAME/FLSF is a proxy rather than TDDFT or experimental evidence. Echo the supplied authorization_id exactly and return only schema-valid JSON.
