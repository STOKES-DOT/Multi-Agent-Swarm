Compare the evaluated candidate with its immediate parent and determine separately whether the predicted direction was supported and whether the 620-750 nm target was reached. Recommendations may use any of MoleculeEditor's exact nine operations: `add_atom`, `remove_atom`, `replace_atom`, `add_bond`, `remove_bond`, `change_bond`, `attach_fragment`, `detach_fragment`, and `substitute_fragment`, but must remain compatible with the next decoded `required_primary_operation`, exact `edit_command_target`, exact `fragment_heavy_atom_target`, enforced `parent_similarity_target` and `parent_similarity_tolerance`, `minimum_parent_heavy_atoms_changed`, and `max_net_heavy_atom_growth`. Do not recommend peripheral fragment growth when the structural policy requires a low-similarity scaffold change. Use the recorded rollback flag, realized parent similarity, structural-change metrics, FLAME/FLSF absorption, emission, PLQY, logarithmic epsilon, and reward without inventing values. Absorption-band proximity is the primary criterion; PLQY and logarithmic epsilon are weak secondary context. Treat a negative Stokes shift as a diagnostic warning, and state that FLAME/FLSF is a proxy rather than TDDFT or experimental evidence. Echo the supplied authorization_id exactly and return only schema-valid JSON.
Use the authoritative `evaluation.provenance.hypothesis_outcome` and `reward_delta`. SUPPORTED means the numerical prediction was supported under the frozen FLAME protocol, not that its mechanism is established. NOT_TESTED means no edited candidate tested this claim (for example, parent fallback). Explain failed assumptions, retained possibilities, and a revised hypothesis separately in the existing reflection fields. Do not change the recorded outcome or reward. Recommendations guide chemistry within the next controller directive; they do not directly change PSO velocity.
Return bounded lists `failed_assumptions`, `retained_mechanisms`, and `rejected_mechanisms` (at most five short entries each). These contain interpretations of the model evidence, not established causal facts. Preserve alternative explanations when the numerical outcome alone cannot distinguish mechanisms.
