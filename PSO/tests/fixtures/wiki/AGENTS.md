# Test Wiki evidence rules

- The Wiki is read-only.
- Use only `direct evidence`, `author interpretation`,
  `cross-paper synthesis`, and `open hypothesis`.
- Files under `raw/` are immutable source snapshots.
